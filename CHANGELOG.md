# 更新日志

## 1.7.1 — 2026-08-13

### 修复

- 修复将标签拖到最右侧时，虽然显示蓝色插入线，但松手后未实际移动到末尾的问题。
- 最右侧空白区域及相邻的新建标签按钮现在共用可靠的末端拖放落点，并兼容 WebView2 的拖拽事件顺序。
- 标签拖拽调整顺序后立即保存；重新启动软件时恢复退出前的标签顺序和前台标签。
- 串行保存标签会话快照，避免较早的异步保存覆盖最新顺序或激活状态。
- 修复启动恢复标签期间将最后加载的标签错误保存为前台标签的问题。

## 1.7.0 — 2026-08-13

### 新功能与交互

- 新增类 Notion 的内容块多选：可从正文左侧空白区域拖动选择多个内容块，选区从实际内容边缘开始，并使用圆角蓝色高亮。
- 内容块左侧新增固定的添加与拖拽入口；控件不参与文本选择，拖拽菜单首项统一为复制，并保留内容块自身适用的操作。
- 统一左右侧栏的展开、收起交互及宽度；收起后整条侧栏均可点击展开，按钮位置保持稳定。
- 左侧目录打开时默认定位当前标签所在目录，并改进文件与文件夹图标辨识度。
- 查找与替换共用展开栏，再次点击任一入口均可收起。

### 视觉与布局

- 全面统一标签栏、工具栏、侧栏和底栏的高度及按钮规格。
- 使用统一正方形画布的新版图标，按视觉重量校准字体格式按钮，避免拉伸和尺寸失衡。
- 优化底部设置、同步按钮与状态栏布局；同步按钮始终显示，未配置或未验证时置灰。
- 移除重复的根目录迁移入口，并调整新建文件、目录与侧栏折叠按钮位置。

### 修复

- 修复左右侧栏展开、收起按钮失效的问题。
- 修复部分左侧空白区域无法拖动多选内容块的问题。
- 修复按钮和交互区域文字可被误选中的问题。
- 修复查找已展开后点击替换（或反向操作）无法收起共用面板的问题。

## 1.6.0 — 2026-08-10

### 新功能

- 新增每日自动更新：每天首次启动检查 GitHub Releases；发现新版本后在后台下载并校验 SHA-256，准备完成后可在左下角确认升级并自动重启、替换程序和删除旧版本。
- 左侧栏底部新增“同步”按钮；连接测试通过且同步配置有效时显示，与“设置”按钮各占一半宽度。
- 代码块右下角新增语法选择器，首批支持纯文本、SQL 和 Java，并保存 Markdown 围栏语言及显示语法高亮。
- 表格支持内容区和边缘区分流：段落文本行使用 I 型光标进行编辑；单元格 padding 使用箭头，可点击或拖动形成矩形多单元格选区。
- 多单元格选区新增整体蓝色外框，以及复制、清空、删除所选行列操作。
- 表格菜单新增复选框列；改进表格行列增删、尺寸拖动和移除范围预览。
- 图片右键菜单新增“在文件管理器中打开”。

### 修复与改进

- 修复表格文本选择与单元格拖选互相抢占鼠标手势的问题；一次手势在按下时确定唯一处理者。
- 修复新增表格行列时未拖动便松开无法增加一行或一列的问题。
- 修复表格列移除预览在横向滚动或非均匀列宽下位置不准确的问题。
- 修复 UTF-8 BOM 文件首行标题无法正确解析的问题。
- 优化侧栏、设置、图片菜单、表格工具栏和中英文界面文案。

### 更新安全

- 更新下载只有在文件大小和 Release SHA-256 校验通过后才进入可安装状态。
- 更新时保留 `config.ini`、`syncstate.json` 和笔记目录；存在未保存内容时阻止自动重启。
- 新版本通过独立更新进程等待旧进程退出后替换，启动成功后自动清理旧 exe 和暂存文件。
