# 开发日志

## 环境信息（记录备查）

- 系统：Windows 11 Pro（Win10+ 目标）
- Node v24.14.0 / npm 11.17.0，安装于 `D:\Program\nodejs`
- Rust：**原环境未安装**，已装至 `D:\Program\Rust`（`CARGO_HOME=D:\Program\Rust\cargo`，`RUSTUP_HOME=D:\Program\Rust\rustup`），`rustc 1.97.1` / `cargo 1.97.1`，已写入用户级 PATH/环境变量（`setx`/`[Environment]::SetEnvironmentVariable(...,"User")`）
- MSVC Build Tools：`C:\Program Files (x86)\Microsoft Visual Studio\2022\BuildTools`，已含 `Microsoft.VisualStudio.Component.VC.Tools.x86.x64`（无需重装）
- WebView2 Runtime：已装，版本 149.0.4022.69
- 项目脚手架：`create-tauri-app` 生成 Tauri 2 + Svelte 5 + TypeScript + Vite 模板
- 已装前端依赖：`@tiptap/core@3.28.0`、`@tiptap/starter-kit@3.28.0`、`@tiptap/pm@3.28.0`、`tiptap-markdown@0.9.0`（peerDep 官方声明兼容 `@tiptap/core ^3.0.1`，满足需求的 Tiptap 3.28+）

## 踩过的坑

1. **`npm create tauri-app@latest -- --name X --identifier ...` 参数解析异常**：CLI 实际不支持 `--name` 选项（项目名是位置参数 `PROJECTNAME`），传入后被误当成目录名，生成了一个字面量叫 `--name` 的目录。修正：改用位置参数，`npx create-tauri-app@latest . --manager npm --template svelte-ts --identifier com.mdnote.app --yes --force`，用 `.` 直接在当前目录（已有 need.md 等文件，非空目录需加 `--force`）生成。
2. **联网失败重试**：首次 `rustup-init.exe` 下载 channel 元数据时报 `os error 10013`（防火墙尚未放行），按 `dev规范.txt` 规则新开一个 PowerShell 进程、等待 10 秒后重试，第二次直接成功（Rust 1.97.1 安装完成）。**结论：本机确实存在"同进程首次联网请求会被拒，需换新进程重试"的现象，后续联网操作若失败应默认走这个重试流程，不要反复在同一进程里重试。**
3. **MSVC 链接器被 Git 自带的 `link.exe` 覆盖**：`cargo build`（`tauri build --debug`）报 `error: linking with link.exe failed`，日志显示实际调用的是 GNU coreutils 的 `link`（"extra operand" 是 coreutils `link(1)` 的报错特征，不是 MSVC link 的报错格式）。根因：`D:\Program\Git\usr\bin\link.exe`（Git for Windows 自带的类 Unix `link` 命令）在 PATH 中排在 MSVC 的 `link.exe` 之前，rustc 调用 `link.exe` 时被错误解析到这个文件。
   **解决方案**：新建了可复用脚本 `scratchpad/msvc-env.ps1`，通过调用 `vcvarsall.bat x64` 抓取 `INCLUDE`/`LIB`/`PATH`（含正确的 MSVC `link.exe`、Windows SDK 路径）注入当前 PowerShell 进程环境变量，再把 `D:\Program\Rust\cargo\bin` 和 `D:\Program\nodejs` 前置到 PATH（因为 `beforeBuildCommand`/`beforeDevCommand` 需要跑 `npm run build`/`npm run dev`）。**之后所有涉及 `cargo build` / `tauri build` / `tauri dev` 的命令都要先 dot-source 这个脚本**，否则会再次被 Git 的 `link.exe` shadow。
   （未对用户级全局 PATH 做永久改动来解决此问题，避免影响 Git 在其他场景下的正常使用；只在构建所需的单次进程环境里生效。）

## 任务节点状态（Tauri 阶段，已废弃）

- [x] 0. 回写 need.md（补充多标签 `+` 按钮 / 中键关闭 / 脏数据确认细节）
- [x] 1. 环境搭建：Rust 安装、MSVC 确认、Tauri2+Svelte+TS 脚手架、Tiptap 3.28 相关依赖安装
- [ ] 2~7：未开始即被架构决策叫停，见下方 "改用 Go + Wails" 记录

## 2026-07-23：改用 Go + Wails，一次性推进到可测试版本

用户要求把外壳/后端从 Tauri(Rust) 换成 Go 实现，优先保证 exe 体积、内存、CPU 尽可能小；并要求整体 UI 尽量还原 Notion 风格；并明确指示"除非遇到问题，否则一路做到可用测试版 exe 为止"，因此本次未按 `dev规范.txt` 的 Preview-First 流程逐步暂停确认，而是直接产出可运行 exe 供用户验收（后续如需，再回头补 Preview 环节）。

**技术选型**：Go + Wails v2（继续复用系统 WebView2，不内置 Chromium）。放弃"纯原生 Go GUI（Fyne/Gio）"方案，因为会丢失 Tiptap/ProseMirror 的 WYSIWYG 编辑能力，需要用户在两条路线间二选一时已确认选择 Wails。

**目录重构**：
- 删除 `src-tauri/`（Rust 后端）。
- 前端整体从仓库根目录搬到 `frontend/`（Wails 约定的前端目录），`package.json` 移除 `@tauri-apps/*` 依赖。
- 根目录新增 Go 模块：`main.go`（窗口/embed 配置）、`app.go`（对话框 + 设置绑定方法）、`fsops.go`（目录树 CRUD）、`settings.go`（便携配置读写）、`wails.json`。
- `svelte.config.js` 的 `adapter-static` 输出目录改为 `dist`（匹配 `main.go` 里 `//go:embed all:frontend/dist`）。

**已实现功能**（一次性写完，非分步 Preview）：文件系统绑定（`ListDir/ReadFile/WriteFile/CreateEntry/RenameEntry/DeleteEntry/MoveEntry`，均校验落在根目录内）、设置持久化、左栏目录树（懒加载 + 右键菜单 + 拖拽移动 + markdown-only 过滤 + 隐藏 `.md` 后缀）、多标签（脏标记/中键关闭/关闭确认）、顶部工具栏 8 图标、Tiptap 编辑器（StarterKit + tiptap-markdown + 自定义查找高亮 ProseMirror 插件）、右栏大纲、底部字数统计、查找替换面板、Notion 风"块添加"浮动按钮 + 块类型菜单、Notion 风视觉样式（浅灰侧栏/无边框图标按钮/圆角卡片弹窗）。

**踩过的坑**：
1. Svelte 5 编译器禁止变量名以 `$` 开头（与 rune 语法冲突），ProseMirror 惯用的解构写法 `const { $from } = selection` 在 `.svelte` 文件里会编译报错 `dollar_prefix_invalid`，需重命名为 `const { $from: sel } = selection`。
2. `npm install` 因 `allow-scripts` 策略拦截了 `esbuild` 的 postinstall（原生二进制下载脚本），需要在 `package.json` 显式加回 `"allowScripts": { "esbuild@<版本>": true }`（版本号需对应 `npm install` 实际解析出的 esbuild 版本，随依赖更新可能变化）。
3. **截图验证时的 DPI 虚拟化坑**：本机系统缩放 150%。用非 DPI-aware 的 PowerShell 进程调用 `GetClientRect`/`PrintWindow` 截取一个已声明 per-monitor-v2 DPI-aware 的窗口（Wails 默认生成的 manifest 已声明），会拿到被虚拟化缩小的窗口尺寸，导致 `PrintWindow` 只截到左上角一部分，右侧大纲栏、底部状态栏"看起来没渲染"——其实是截图脚本的坐标系问题，不是应用 bug。修复：截图前对当前线程调用 `SetThreadDpiAwarenessContext(DPI_AWARENESS_CONTEXT_PER_MONITOR_AWARE_V2)`（值 `-4`），再读 `GetClientRect`/`PrintWindow` 即可拿到真实物理像素尺寸和完整内容。**后续任何要截图验证 Wails/WebView2 窗口的场景都应先设置这个 DPI 上下文，否则会得出"某块 UI 没渲染"的假阴性结论。**
4. `SetForegroundWindow` / `WScript.Shell.AppActivate` 在自动化环境里可能静默失败（Windows 焦点抢占保护），此时对目标 hwnd 截图可能实际截到桌面上其它无关窗口的内容而不报错——因此窗口截图一律优先用 `PrintWindow`（直接从窗口自身取内容，不依赖前台焦点/坐标是否被其它窗口遮挡），比"前置窗口 + 截屏"更可靠。
5. 测试中发现一次 `build\bin\data\settings.json` 出现了非预期的 `rootDir`（指向一个真实存在的用户目录），具体触发路径未完全定位（怀疑与前几次窗口激活尝试有关），但确认代码里没有任何写死的默认路径、也没有自动写入逻辑。每次测试后已手动清空 `build\bin\data`；如后续复现，需要重点排查 `runtime.OpenDirectoryDialog` 在窗口未正确获得前台焦点时是否会静默返回系统缓存的"上次选择目录"而不是真正弹窗等待用户选择。

**产物**：`wails build -clean -ldflags "-s -w"` → `build\bin\mdnote.exe`，约 11.4MB（未装 `upx.exe`，未做进一步压缩）。已实机启动 + `PrintWindow`（DPI-aware）截图确认：目录空态、标签栏（已移到最上方）+ 工具栏（已移到标签栏下方）、大纲栏、状态栏、编辑区"块添加"浮动按钮均正常渲染。

## 2026-07-23（续）：用户测试第一轮反馈的 6 个 bug 修复

1. **新建空文件夹在左栏不显示、刷新无效**：根因是 markdown-only 过滤规则本身——"目录不含 md 递归子项就隐藏"对刚创建的空文件夹同样成立，所以新建出来的空文件夹立刻被过滤掉；刷新按钮其实工作正常，只是重新算出了同样的过滤结果。修复：`fsops.go` 新增 `shouldShowDir`，目录为"真正空"（一个子项都没有）时也展示，不再要求必须已含 `.md`。
2. **悬浮"+"点了没弹出菜单**：块菜单按钮/菜单项都在 ProseMirror 的 contenteditable 区域之外，点击时浏览器先在 `mousedown` 阶段把编辑器 blur 掉，而 `onBlur` 回调里 `if (!menuOpen) handleTop = null` 判断的是"blur 发生那一刻"的 `menuOpen`（此时点击的 `onclick` 还没来得及把它设成 true），于是按钮和菜单在真正触发点击前就被判定为要隐藏。修复：给按钮和每个菜单项加 `onmousedown` 里 `preventDefault()`，阻止这次点击引发编辑器失焦。
3. **新增：光标所在空行显示浅色提示"按 / 或空格快速插入格式"**：直接使用 `@tiptap/extensions` 里官方的 `Placeholder` 扩展（默认 `showOnlyCurrent: true`，正好只在当前光标所在空行显示），加了对应 `.is-empty::before` 样式。
4. **redo 无内容时应置灰**：`editorBridge` 新增 `canUndo`/`canRedo` 两个响应式字段，编辑器每次 `onTransaction` 都调用 `editor.can().undo()/.redo()` 同步；工具栏按钮据此加 `disabled`。
5. **撤销把所有内容一次性撤没**：定位到真实 bug——`editor.commands.setContent(content, false)` 里第二个参数在当前 Tiptap 版本已经是"选项对象"而不是布尔值（`{ emitUpdate = true, ... } = {}` 解构），传 `false` 进去完全不生效、`emitUpdate` 仍按默认 `true` 处理；更关键的是这次内容替换的事务从未打上 `addToHistory:false`，导致每次切换标签页/打开文件，都会把"整篇替换"当一步推进撤销历史——只要用户后续撤销几次就会命中这一步，表现为"内容整体消失"。修复为 `editor.chain().setMeta("addToHistory", false).setContent(content, { emitUpdate: false }).run()`，正确使用选项对象且不再污染撤销历史。未改动默认 500ms 的按键分组间隔（`newGroupDelay`）——这是几乎所有编辑器的标准行为（连续快速输入算一步撤销），如果用户验证后仍觉得需要"每个字符单独一步撤销"，需要再单独调整。
6. **关闭未保存标签的"不保存"按钮改名"舍弃"+红色**：`ConfirmDialog.svelte` 按钮文案改为"舍弃"，加 `class="danger"`（复用删除确认已有的红色样式）。

修复后已 `wails build -clean -ldflags "-s -w"` 重新产出 `mdnote.exe`（约 11.4MB）并截图确认：占位提示文本、撤销/重做置灰态均正常渲染。

## 2026-07-23（再续）：拖拽改指针事件 + 新建改内联输入框 + 设置改 INI

在未逐条记录的一次会话里已完成：
- 目录树拖拽排序/移动改用指针事件（`dragController.ts` + `TreeNode.svelte` 的 `onpointerdown`），原因见下一条"踩过的坑"；`fsops.go` 配套改动、`Sidebar.svelte` 根目录也接入同一套 hit-test 作为拖放目标。
- "新建文件/文件夹"从 `window.prompt` 改成内联输入框（`NewEntryRow.svelte` + `appState.beginCreateEntry/cancelCreateEntry/commitCreateEntry`），未提交前不写盘。
- `go build` 与 `npm run check` 均确认无编译/类型错误，`wails build -clean -ldflags "-s -w"` 重新产出可运行 `mdnote.exe`。

**踩过的坑（补记）**：WebView2 对原生 HTML5 Drag and Drop 的 `dragstart`/`dragover`/`drop` 事件长期存在平台 bug，同样的标记在真实浏览器里能触发、在 WebView2 里完全不触发，因此改为纯指针事件（`pointerdown` 开始跟踪、`pointermove` 过阈值后用 `document.elementFromPoint` 做命中测试、`pointerup` 提交移动）。

本轮新增：设置持久化格式从 `<exe目录>\data\settings.json`（JSON）改为 `<exe目录>\mdnote.ini`（INI，`[General]` 节 `RootDir=` 键），用户可直接用记事本打开确认/修改左栏目录树当前指向的系统路径，不再需要进 `data\` 子目录翻 JSON。`settings.go` 手写了极简 INI 读写（只有一个 key，未引入第三方 ini 库）；Go 结构体字段仍保留 `json:"rootDir"` tag——它现在只用于 Wails 生成前端 TS 绑定时的字段命名，与文件存储格式无关。`.gitignore` 的 `/data/` 规则相应改为 `/mdnote.ini` + `/build/bin/mdnote.ini`。

## 2026-07-23（再再续）：目录树取消 markdown 过滤 + 打开文件默认目录

用户测试反馈 2 点：

1. **左栏目录树应展示根目录下所有文件夹层级，不管是否含 md 文件、是否本软件创建**：`fsops.go` 的 `ListDir` 原先只在子目录"真正空"或"递归含至少一个 .md"时才展示（见上上一轮"新建空文件夹不显示"那次修复），但这个规则本身仍会隐藏"含其它类型文件但不含 md"的目录（比如只放了图片的文件夹）。修复：删除 `shouldShowDir`/`dirHasMarkdown` 这两个过滤函数，`ListDir` 现在无条件展示所有子目录；文件仍只展示 `.md`（非 md 文件不在树里显示，未被要求展示）。
2. **未选中任何左栏节点时，"打开文件"对话框、"另存为"对话框的默认目录都应该是设置里的根目录**："另存为"（`saveFileDialog`）本来就已经通过 `appState.targetDirForNewEntry()` 做到了（选中目录用选中目录/选中文件用其所在目录/未选中用根目录）。"打开文件"（工具栏"打开"按钮、以及斜杠菜单里的"page link"选择器，两者都调用 `api.openFileDialog`）之前完全没有默认目录参数。修复：`app.go` 的 `OpenFileDialog` 新增 `defaultDir string` 参数（复用 `SaveFileDialog` 已有的"存在则用、否则回退系统默认"逻辑），`api.ts`/`appState.svelte.ts` 的两处调用点都改传 `this.targetDirForNewEntry()`。

均已 `go build`/`npm run check` 确认无错误，`wails build` 重新产出 exe 并用测试根目录（含一个只放图片的文件夹）截图确认过滤规则已生效。

## 2026-07-23（第四轮）：图标 + 用户测试反馈 8 个问题

**软件图标**：换成白底黑色"M"（做法：绘制黑色粗体"W"路径，垂直翻转变成"M"，而不是直接画个 M 字形——用户明确要求"W 倒转"这个具体做法），替换掉原来的蓝底白字 M。`build/appicon.png`（1024×1024）、`build/windows/icon.ico`（16/32/48/64/128/256 多尺寸，手工拼装 PNG-in-ICO，未装 ImageMagick 之类工具）、`frontend/static/favicon.png` 三处一并换新。

用户测试反馈 8 个问题，逐一定位修复：

1. **打开 md 文件后窗口取消最大化**：根因是应用从未记住过窗口的最大化状态——`main.go` 里 `WindowStartState` 一直没设置，每次启动/新开窗口都固定用默认的 1280×800，用户之前手动最大化过也不会被记住。修复：`settings.go` 的 `Settings` 加 `WindowMaximized` 字段（ini 里 `Maximized=` 键），`app.go` 新增 `onBeforeClose`（关闭前用 `runtime.WindowIsMaximised` 读取当前状态存盘），`main.go` 启动时读取该值决定 `WindowStartState` 是否为 `options.Maximised`。`SelectRootDir` 原来存盘时是 `Settings{RootDir: dir}` 直接覆盖整个结构体，会把 `WindowMaximized` 一起清零，顺手改成先 `loadSettings()` 再只改 `RootDir` 字段。已实机验证：ini 里手写 `Maximized=true` 启动后 `IsZoomed` 为真；取消最大化后关闭，ini 自动写回 `Maximized=false`。
2. **打开文件后未修改也显示脏点**：根因是 `Editor.svelte` 的 `syncFromEditor()` 在"刚加载/切换标签"时也会被调用，它会把 `tiptap-markdown` 重新序列化出的 markdown 写回 `tab.content`——而这个"编辑器→markdown"往返序列化不保证和原始文件字节完全一致（列表符号、标题写法、尾随换行等归一化），于是 `tab.content` 和 `tab.savedContent`（原始文件内容）出现细微差异，被判定为"脏"。修复：拆出一个只刷新大纲/字数、不touch `tab.content` 的 `refreshDerivedState()`，加载/切换标签时改调它；只有真正的用户编辑（`onUpdate`）才会调用会重写 `tab.content` 的完整 `syncFromEditor()`。
3. **新增：双击标签重命名**：`Tabs.svelte` 加 `renamingId`/`renameValue` 状态，双击标签标题进入编辑；未命名过的空白标签（仍是默认"未命名.md"）→ 输入框留空聚焦；已有实际名字（真实文件，或之前改过名的空白标签）→ 全选文字方便直接覆盖。有 `path` 的标签提交时走 `appState.renameEntry`（真正改磁盘文件名，和左栏重命名同一套逻辑，保持标签与文件同步）；没有 `path` 的未保存标签只改内存里的 `tab.title`——由于"另存为"早就用 `tab.title` 当默认文件名（见上一轮"根目录默认目录"那次改动），改完标题后另存为自动就会用新名字。
4. **标签拖拽只能左移到第一个，不能右移到最后一个**：原生 HTML5 拖拽的 `ondragover`/`ondrop` 都绑在具体的标签按钮上，最后一个标签右侧的空白区域没有任何元素能接住 drop，所以"拖到最后"这个手势没有落点。修复：`.tabs-scroll` 末尾加一个 `flex:1` 的占位 `div` 承接 dragover/drop，触发 `appState.moveTabToEnd`。（顺带发现 Tabs.svelte 是唯一还在用原生 HTML5 DnD 的地方——和之前判定"WebView2 原生拖拽事件完全不触发"不同，这里显然是能收到事件的，只是缺少末尾落点；说明那条结论对标签栏不成立，具体原因未深究，但既然现状可用就不再额外改造成指针事件。）
5. **左栏拖拽的 before/after 位移线是黑色，应为蓝色**：`TreeNode.svelte` 的 `.row.drag-before::before`/`.row.drag-after::after` 背景色从 `var(--text-primary)` 改成 `var(--accent)`，和其它蓝色高亮统一。
6. **点击文件夹名称经常不展开，只有点箭头才行**：根因是"点击展开"的 `onclick` 只挂在 `.row-label` 这个紧贴文字的 `<span>` 上，而不是整行——鼠标点在行内文字周围的留白（视觉上看着像"点了文件夹名"，实际落在 `.row` 背景而非文字 span）时没有任何处理器响应。修复：把 `onclick={onRowClick}` 移到整个 `.row` div 上（chevron 自己的 `onclick` 已有 `stopPropagation`，不会双重触发 toggle）。
7. **根目录信息应该显示在最顶部**：`Sidebar.svelte` 原来只在最底部一个不起眼的按钮里显示根目录路径。改成在 `.tree` 顶部加一个常驻的根目录行（文件夹图标 + 目录名，hover 出现"更换根目录"小图标按钮），其余目录/文件全部缩进一级显示在它下面；原来的底部 `.sidebar-footer` 整块删除，避免信息重复。
8. **编辑区拖拽"⠿"六点手柄无法移动内容块**：和标签栏不同，这里确认是同一个 WebView2 原生 HTML5 DnD 不触发的问题（`Editor.svelte` 之前 `draggable="true"` + `ondragstart`/`ondragover`/`ondrop`，和左栏文件树重构前的写法一模一样）。修复：按 `dragController.ts` 的思路重写成纯指针事件（`onpointerdown` 起始跟踪、`pointermove` 过阈值后用 `editor.view.posAtCoords` 命中测试、`pointerup` 提交移动），拖拽高亮/落点判定逻辑（`block-drag-source`/`block-drop-target`）保持不变，只是触发方式从原生拖拽事件换成手动指针事件。

修复过程中还发现一个连带 bug：给 `Tabs.svelte` 加 `$state` 用于重命名功能后，组件整体切换到 Svelte 5 runes 模式，暴露出原来 `draggingId`/`dragOverId` 是普通 `let`（非 `$state`）——在 legacy 模式下这类顶层变量隐式响应式，一旦文件因为其它 `$state` 用法被强制转成 runes 模式，普通 `let` 的重新赋值就不再触发界面更新了（`svelte-check` 报 `non_reactive_update` 警告）。顺手把这两个变量也改成了 `$state`，避免标签拖拽的高亮/占位反馈静默失效。

`go build`、`npm run check` 均确认 0 错误，`wails build` 重新产出 exe。已用测试根目录截图确认过：新图标（标题栏左上角）、根目录顶部展示（含子目录正确缩进）两处静态可见的改动；窗口最大化记忆用 `IsZoomed`/直接改写 ini 两种方式验证了读取和写回两个方向都工作正常。其余需要真实鼠标交互才能看到效果的改动（重命名、拖拽反馈、点击展开、块拖拽）**本环境无法可靠模拟点击验证**（见前一轮记录），逻辑走查后按最小风险改法实现，建议用户实机操作确认。

## 2026-07-23（第五轮）：多轮用户测试反馈汇总 + 根目录改成 MDNote 子文件夹架构

**"幽灵 rootDir" 之谜已解开，不是 bug**：之前怀疑 `OpenDirectoryDialog` 会静默返回缓存目录，实际是本机单实例机制（`singleinstance.go`，全局固定端口 127.0.0.1:57391）把开发过程中反复的测试启动"接"到了用户自己真实在用的那个窗口上——用户真实配置的根目录就是 D:\Download，不是软件凭空造出来的。之前几轮排查方向绕了弯路，教训：以后测试前先确认没有别的 mdnote 进程占着单实例端口。

**多轮零散反馈修复**（未逐条记录过程，汇总结果）：
- 另存为默认文件名去掉 `.md`（`stripMdExt`），Go 侧 `SaveFileDialog` 默认名同步改成不带扩展名；确认文件名框仍会显示 `.md` 是 Windows/Wails 保存对话框 `DefaultExtension` 机制的固有行为，不完全可控。
- 标签栏：加最小/最大宽度、标题居中+省略号+原生 tooltip、当前标签蓝色下划线区分、拖到最后一个的视觉提示改成挂在末尾占位区而不是最后一个标签左边。
- 左栏拖拽：拖folder只保留"移入"这一种交互（去掉了之前加的"经过上层文件夹边缘出现平级横条"的设计，用户明确反馈这个交互点不该存在）、拖拽时鼠标全程显示抓取光标（之前只有源行自己有 cursor 样式，鼠标移开就没了）、拖拽时跟随光标显示半透明"幽灵"卡片（文件名+图标）。
- 编辑器块级"+"/拖拽手柄：改成跟随鼠标实际悬停行定位（之前长块如展开的折叠块内容很长时，手柄固定在块顶部，滚动到块下方就等于视觉消失）；拖拽手柄按下时的取块逻辑之前要求 `hoverBlockPos` 非空，但手柄在"键盘/光标 fallback"显示时这个值可能是 null，导致按住拖拽完全没反应——已改成和显示逻辑一致的 fallback。
- 分割线插入后按空格不出现斜杠菜单：定位到根因是插入的分割线是纯 atom 节点、没有可选中的内部位置，`setTextSelection` 静默失败后选区状态不对，导致空格触发条件（光标必须在空段落里）不成立。改成分割线插入时固定带一个紧跟其后的空段落，并把三处手动 `setTextSelection` 全部换成更健壮的 `TextSelection.near`。
- 高亮块（callout）回车/shift+回车混乱：schema 允许 `block+`（多段落），默认回车只会在块内再拆一段，从不跳出，行为对不上预期。给 callout 加了显式回车处理：回车固定跳出到块后（没有下一个块就新建一个空段落），shift+回车保持 StarterKit 默认的软换行不动。
- 左栏点文件夹展开点击区域从"仅文字"扩到"整行"；拖拽位移蓝线从黑改蓝、长度随目标行缩进变化。
- 移动一个"当前正展开"的文件夹时偶发"读取目录失败"提示：根因是移动后全局刷新信号会让这个文件夹自己（还没来得及被卸载）用旧路径重新读取一次，必然 404。改成这种全局背景刷新触发的失败不弹提示，用户主动点开失败时才提示。
- 窗口最大化状态在"重复启动/双击第二个文件转发到已运行实例"这条路径上做了防御性纠正（不确定是否是用户实际触发路径，已在 `notifyOpenFile` 里补了"发现意外变成非最大化就强制改回来"的兜底）。

**新增架构改动：根目录不再直接当笔记目录用**：用户要求选根目录后不要直接把里面的系统文件都摆出来，而是在选定目录下固定建一个 `MDNote` 子文件夹，左栏只显示"用户选的那个父目录"路径（不显示"MDNote"这一级），实际读写全部落在 `父目录\MDNote` 下。实现：
- `fsops.go` 新增 `effectiveRoot(Settings) string`（= `RootDir\MDNote`），`ListDir/CreateEntry/RenameEntry/DeleteEntry/MoveEntry` 里所有 `withinRoot` 校验都从直接用 `settings.RootDir` 改成用 `effectiveRoot(settings)`。
- `app.go` 的 `SelectRootDir` 选定目录后 `os.MkdirAll` 保证 `MDNote` 子文件夹存在。
- 新增 `MigrateRootDir`：弹目录选择框，把当前 `MDNote` 整个文件夹搬到新父目录下（`os.Rename`，跨盘符会失败，已加 `moveDirAcrossVolumes` 回退成"递归复制+删除源"）。
- 前端 `appState.effectiveRootDir` getter（= `settings.rootDir + "\\MDNote"`），所有实际文件操作（`listDir`/`targetDirForNewEntry`/拖拽根目标/选中根目录）改用这个而不是 `settings.rootDir`；`rootDirName`（左栏顶部显示的名字）继续用 `settings.rootDir` 派生，不受影响。
- Sidebar 根目录行新增"迁移"按钮（图标新增 `move`），迁移后同步把所有打开标签页的路径前缀从旧 `effectiveRootDir` 换成新的，不用整批关闭标签。
- 已用测试目录截图验证：父目录标签正确显示为选中的目录名（不是"MDNote"），树内容正确对应 `父目录\MDNote` 下的文件/文件夹，父目录下故意放的"兄弟文件"确认不出现在树里。`SelectRootDir`/`MigrateRootDir` 因为要走原生目录选择框，无法在本环境里自动化验证，需要用户实机测试。

`go build`、`npm run check` 均 0 错误。为诊断"编辑器内容区拖拽按钮无法拖拽"这个反复出现的问题，临时在 `app.go` 加了 `DebugLog` 方法（写 `debug.log` 到 exe 同目录），已在 `Editor.svelte` 关键节点埋点，等用户下次实机复现后读日志定位——定位完成后需要记得删除这段临时诊断代码。

## 待处理 / 待用户确认

- 本次为一次性直达可测试版本，尚未按 `dev规范.txt` 的 Preview-First 流程走查视觉稿；如需补齐规范流程，可后续单独过一遍 Preview/Playground 环节。
- 体积/UPX 压缩、安装包（用户已明确表示不需要 UPX、便携版即可）等收尾项基本不再需要；图标已换新。
- exe 现在交替产出到 `build\bin\mdnote.exe`/`mdnote2.exe`（用户要求），构建命令注意用 `-o` 指定输出名、不要带 `-clean`（会连另一个文件也删掉）。

## 2026-07-24：块拖拽 bug 根因定位 + 文件夹重命名报错 toast

用户反馈"内容区域/内容块还是无法实现拖拽移动"——上一轮虽然把块拖拽从原生 HTML5 DnD 改成了指针事件（能触发了），但读取 `DebugLog` 上一次实机测试留下的 `build\bin\debug.log` 发现移动过程中 `updateDropTarget` 里的 `editor.view.posAtCoords` **每一次 pointermove 都返回 null**，导致 `dropTargetPos` 永远是 null、`commitBlockMove` 永远拿不到落点，拖拽等于什么都没发生。

**根因**：查了 `prosemirror-view` 源码（`posAtCoords` 实现），坐标一旦落在 `view.dom.getBoundingClientRect()`（即 `.tiptap` contenteditable 元素自身的渲染框）之外就直接返回 null。而 `.tiptap` 的高度只等于其实际内容高度，外层 `.editor-content-col-wrapper` 却为了"点击末尾空白也能落光标"这个体验特意留了 `padding: 56px 64px 30vh`——只要拖拽时鼠标移到最后一行下方（或短文档场景下很容易发生），坐标就落在这段空白 padding 里，落在 `.tiptap` 框外，`posAtCoords` 必然返回 null。侧栏文件树的拖拽用的是 `document.elementFromPoint`（永远能命中点什么，包括空白容器），所以那条路没有这个问题；块拖拽换成指针事件后事件本身能触发了，但命中测试用的是 ProseMirror 自己的 `posAtCoords`，这个函数的"越界即 null"特性在上一轮改造时没被注意到，是两条拖拽实现路径行为不一致的根本原因。

**修复**：`Editor.svelte` 的 `updateDropTarget` 在调用 `posAtCoords` 前，先把 `clientX/clientY` 钳制（clamp）到 `editor.view.dom.getBoundingClientRect()` 内侧 1px，这样拖到内容下方/左右留白时会落到最近的真实文档位置而不是直接判 null。诊断用完后清理了 `DebugLog`（`app.go`）、`api.debugLog`（`api.ts`）、`Editor.svelte` 里的埋点调用，以及 `build\bin\debug.log`。

同一轮用户还反馈"重命名文件夹时，名字改成功了，但底部还是弹错误 toast"。定位：`TreeNode.svelte` 的 `commitRename` 挂在 `<input>` 的 `onkeydown`（Enter）和 `onblur` 两处；按 Enter 时 `commitRename()` 把 `renaming` 置 false，`{#if renaming}` 块随之把这个仍处于聚焦状态的 `<input>` 从 DOM 里摘掉——原生浏览器行为是"聚焦元素被移除会触发一次 blur"，于是 `onblur` 又把 `commitRename()` 调用了第二次。第二次调用时 `entry.path`/`entry.name` 还是重命名前的旧值（父组件的 children 列表要等 `refreshTree()` 异步回来才会用新 path 替换这个节点），于是又拿着已经被改名、此刻已不存在的旧路径发起一次 `RenameEntry`，必然失败，弹出"重命名失败"toast——尽管第一次调用早已成功、UI 上名字也确实改对了。`Tabs.svelte`（标签重命名）和 `NewEntryRow.svelte`（新建文件/文件夹）本来就各自有 `renamingId !== tab.id` / `settled` 这种"重入拦截"，唯独 `TreeNode.svelte` 没加，属于三处本该一致的模式里漏了一处。修复：`commitRename` 开头加 `if (!renaming) return;`，与另外两处的写法保持一致。

`go build`、`npm run check` 均 0 错误，`wails build -ldflags "-s -w" -o mdnote2.exe` 重新产出（`mdnote.exe` 当时正被用户实机运行占用，改产出到 `mdnote2.exe`）。两处改动都需要真实鼠标交互才能验证效果，建议用户直接用 `mdnote2.exe` 实机测试：1）多块文档里用六点手柄把某个块拖到另一个块的上/下方，包括拖到文档最后一块下方的空白区域；2）右键文件夹重命名，确认改名后不再弹"重命名失败"。

## 2026-07-24（续）：三个新反馈 + exe 产出方式改为单文件

用户测试后反馈三点：

1. **左栏重命名文件不应带 `.md` 后缀**：`TreeNode.svelte` 的 `startRename` 原来直接把 `entry.name`（含 `.md`）塞进输入框；改成文件用 `stripMdExt(entry.name)`（复用 `appState.svelte.ts` 已有的这个函数，和标签重命名同款逻辑）、文件夹不受影响（本来就没有扩展名）。`commitRename` 提交时对文件名同步做 `/\.md$/i.test(trimmed) ? trimmed : trimmed + ".md"`，逻辑和 `Tabs.svelte`/`appState.commitCreateEntry` 里已有的"自动补 .md"写法保持一致。

2. **块拖拽仍不能用（第一轮 posAtCoords 越界 clamp 修复不够）**：由于本环境无法用鼠标实机复现拖拽手势，暂时又把上一轮删掉的 `DebugLog` 机制（`app.go` 的 `DebugLog` 方法、`api.debugLog`）临时加回来，这次埋点位置更细：`updateDropTarget` 里 clamp 后 `posAtCoords` 仍返回 null 时记录原始/clamp 后坐标和 `view.dom` 的矩形；`resolved.depth<1` 分支也加了日志；命中成功时记录 `blockPos`；`commitBlockMove` 开头记录 `dragSource`/`dropTargetPos`，`targetPos===null` 和 `node`/`targetNode` 缺失两个提前返回分支也各加了一条日志。已重新产出待用户实机复现后读 `debug.log` 定位——这轮尚未找到根因，是本次会话唯一还开着的诊断任务。

3. **新增需求：`+` 号应随当前行格式变化，点击可"就地转换"格式而不丢内容**：
   - `Icon.svelte` 新增 `h1`/`h2`/`h3`/`h4` 四个用 `<text>` 元素画的字形图标（之前标题 1~4 全部共用和正文一样的 `text` 图标，视觉上根本分不出当前是几级标题，不加这组图标的话"显示当前格式"这个需求对标题类型形同虚设）。
   - `Editor.svelte` 新增 `blockTypeKeyFor(node)`：把光标/悬停所在块的 ProseMirror 节点类型映射到 `blockTypes` 里的 key（heading 按 level 分 h1~h4，paragraph 视为"没有格式"返回 null，其余结构化类型直接按 `node.type.name` 映射）。`updateHandle()` 里用它算出 `handleFormatIcon`（$state），模板里 `+` 按钮图标从写死的 `plus` 改成 `handleFormatIcon ?? "plus"`，hover/title 文案也相应从"添加"变成"更改格式"。
   - 新增 `extractInlineContent(node)`：递归摊平出一个块内部的纯行内内容（文字+行内 mark，比如加粗/链接），无视外层是 listItem/paragraph/toggleSummary 哪种结构包裹；`buildConvertedContent(key, inline)`：和已有的 `buildBlockContent` 结构基本对应，但把 `inline` 塞进目标类型真正装文字的那个位置（heading 直接装、list/callout/blockquote 装进内层 paragraph、codeBlock 特殊处理成纯文本因为代码块 schema 不支持 mark）。
   - `insertBlock()` 分支逻辑：当前块 `blockTypeKeyFor` 非空（说明"有格式"）且非空块、且目标 key 在 `CONVERTIBLE_KEYS`（覆盖 paragraph/h1~4/两种列表/taskList/toggleList/callout/blockquote/codeBlock，不含 table/hr/newPage/pageLink/fileLink 这些没法保内容的结构）→ 走"就地转换"（`insertContentAt({from:pos,to:blockEnd}, buildConvertedContent(...))`）；否则维持原来的老逻辑（空块就地转换 / 非空块插入新块）不变——纯文本段落点 `+` 加内容这个最常用手势完全没受影响。

**exe 产出方式改为单文件**：用户明确要求"每次改完 exe 强制关闭所有正在跑的 exe，只保留一个 mdnote.exe，不用再交替生成 mdnote2.exe"，撤销了 2026-07-23 第四轮记录里"因为旧 exe 被占用而交替产出两个文件"那个变通做法。已存成偏好记忆（`mdnote_single_exe_convention` memory 文件）。后续构建流程固定为：`Get-Process mdnote,mdnote2 -ErrorAction SilentlyContinue | Stop-Process -Force` 杀掉在跑的实例，再 `wails build -ldflags "-s -w" -o mdnote.exe`。

`go build`、`npm run check` 均 0 错误。本轮产出的 `mdnote.exe` 同时含：重命名 `.md` 修复（已实现，逻辑简单，风险低）+ 块拖拽诊断埋点（等用户复现后读日志）+ 格式就地转换新功能（代码走查完成，未做实机点击验证，需要用户实机测试：悬停一个已有格式的非空块——比如打了字的 H2 标题或列表项——确认 `+` 图标变成对应格式图标，点击后选另一种格式，原内容还在且格式已变，而不是掉到下面多出一个新块）。

## 2026-07-24（第三次续）：又提了 3 点反馈（含"格式转换没生效"）

1. **当前 tab 是空白未使用状态时，打开已有 md 文件应顶掉这个空标签，而不是并排开一个新标签**：`appState.svelte.ts` 新增 `isPristineTab(tab)`（`path === null && content === "" && title === "未命名.md"`——三个条件都得满足，用户手动改过标题的空白草稿标签不算"未使用"，不会被顶掉）。`openPath(path)` 在真正发起 `readFile` 之前先记下"当前激活标签是否满足这个条件"，读取成功、新标签推入并激活之后，再 `closeTabImmediately` 那个记下来的旧标签——此时 `activeTabId` 已经指向新标签，`closeTabImmediately` 内部"若关闭的是当前激活标签则顺移" 那段逻辑不会误触发。所有打开文件的入口（左栏点击、"打开"对话框、链接跳转、启动时/单实例转发带的文件参数）都统一走 `openPath`，一处改动全覆盖。

2. **块拖拽应该有跟随鼠标的半透明幽灵 + 蓝色插入位置线，参考左栏文件树已有的同款交互**：之前编辑区块拖拽只有"源块整体半透明( `.block-drag-source` )"+"目标块整体背景高亮（`.block-drop-target`）"，没有跟随鼠标的浮动提示，插入点也只是模糊的"整块高亮"而不是精确的线。改成和 `dragController.ts`/`TreeNode.svelte` 一致的两件套：`Editor.svelte` 新增本地 `blockDragGhost`（文字取该块的纯文本前 40 字，空块则退回显示格式名称，比如"高亮块"），跟随指针移动，样式抄的 `+page.svelte` 里 `.drag-ghost` 那份；`updateDropTarget` 不再套背景色的 `block-drop-target`，改成套 `block-drop-before`/`block-drop-after`（伪元素画一条 2px 蓝线贴在目标块上/下边缘，和 `TreeNode.svelte` 的 `.drag-before`/`.drag-after` 同一手法），先算好 before/after 再决定加哪个 class（原来的写法是先套 class 再算 position，`before`/`after` 之间切换时不会重新触发 class 更新——这次顺带修了这个次序问题）。

3. **反馈"格式转换没实现"**：走查了一遍代码（`blockTypeKeyFor`/`handleFormatIcon`/模板里 `<Icon name={handleFormatIcon ?? "plus"}>` 这几处），逻辑本身没找出明显 bug，怀疑是用户测试时用的还是这轮改动之前产出的旧 exe（这一功能是本轮第一次实现，构建时间点在用户发出这条反馈之前，但客户端切换到新 exe 需要用户手动操作）。本轮已把这功能和另外两点一起打进最新的单文件 `mdnote.exe`，需要用户彻底关闭旧进程、重新用最新 exe 验证：把光标悬停/点击在一个已经打了字的标题或列表项上，看 `+` 图标是否变成对应格式图标（而不是一直显示通用加号）。若最新 exe 上依然不生效，需要用户具体描述"悬停哪种格式的块、图标有没有变、点击后菜单弹没弹出来"，才能进一步定位。

`go build`、`npm run check` 均 0 错误，`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（先 `Stop-Process -Force` 杀掉所有在跑的 `mdnote`/`mdnote2` 进程，按新约定只保留一个 exe）。块拖拽的诊断埋点仍未删除——上一轮的根因还没有定位到，这是本次会话结束时唯一还悬着的任务，需要用户下次用最新 exe 复现拖拽手势后读 `build\bin\debug.log`。

## 2026-07-24（第四次续）：手柄放大 + 插入线改整行覆盖层 + 悬停抖动

用户又反馈 3 点：

1. **正文 `+`/格式/拖拽三个图标太小，不好点**：`.block-handle`/`.drag-handle` 按钮从 22px 放大到 28px（大致对齐正文 `16px` 字号 × `1.6` 行高 ≈ 26px 的视觉行高），图标从 `size=14/15` 提到 `18/19`；`handle-group` 的 `left` 从 4px 收到 2px、按钮间 `gap` 从 2px 收到 1px，两个 28px 按钮 + 1px 间隙一共约 57px，仍然塞得进内容区左侧 64px 的空白（放大前是给 22px 按钮设计的宽裕度，放大后这块空间变紧但够用，没有溢出到文字里）。

2. **拖拽时的蓝色插入位置指示"还是没有显示"，且反馈应改成整行覆盖而不是细线**：怀疑上一版用 `::before`/`::after` 伪元素挂在目标块自己的 DOM 节点上这个思路本身不够稳（不同块类型——`<p>`/`<h2>`/`<ul>`/表格单元格/带自己滚动条的代码块——各自的层叠上下文、`overflow` 设置都不一样，伪元素在某些块类型上可能被裁切或层级被盖住，没法一次性保证每种块都能正常显示）。改成和已经验证能正常渲染的"+"手柄同一套技术：新增 `dropIndicatorTop`（$state，wrapper 相对坐标，和 `handleTop` 同一坐标系），在 `updateDropTarget` 里用目标块的 `getBoundingClientRect()` 直接算出蓝线应该在的 Y 坐标，模板里渲染一个独立的 `<div class="block-drop-indicator">` 绝对定位覆盖层（不再依赖挂在任意块类型 DOM 上的伪元素）。同时改成 `left:0; right:0`（贴 `.editor-content-col-wrapper` 的整个内边距盒宽度，包含左侧手柄所在的留白区），比之前贴着目标块文字宽度的细线更符合"整行蓝色区域"的要求，视觉上也更粗更显眼（4px 高，之前是 2px）。删除了不再需要的 `dropTargetDom`/`clearDropTargetHighlight`/`dropClassFor` 这几个基于 CSS class 切换的旧机制。

3. **列表项：鼠标在文字和前面的 `•` 项目符号之间移动时，手柄上下跳几像素**：根因是原来的定位逻辑（`hoverLinePos` 存的是 `posAtCoords` 解析出的精确文字位置，再用 `editor.view.coordsAtPos(linePos)` 取这个精确字符位置的包围盒）——项目符号本身不是真实的文字内容（是 `list-style` 渲染出来的，不在文档的字符流里），鼠标悬停在符号上时 `posAtCoords` 会捕捉到该行最近的一个文字边界位置（通常是这行文字的起始偏移），而"行首边界位置"和"行中间某个字符"这两种位置在部分浏览器排版引擎下，`coordsAtPos` 返回的包围盒会因为 `line-height` 的行间距分配方式不同而相差几像素——即使两者视觉上明明是同一行。改成不依赖 ProseMirror 解析精确字符位置，而是新增 `lineRectNear(pos, mouseY)`：对目标块的 DOM 内容取一个 `Range`（`range.selectNodeContents(dom)`），用 `Range.getClientRects()` 按实际换行拆出每一视觉行各自的矩形（这个 API 和 `Element.getClientRects()` 不同，后者对块级元素通常只返回整个盒子一个矩形，不会按行拆分），再挑出鼠标 Y 坐标落在其中的那一行矩形直接用——纯几何判断，不再经过"先解析成文字位置、再反查坐标"这一步会引入的边界量化误差。`hoverLinePos`（解析出的文字位置）相应改名替换成 `hoverClientY`（鼠标的原始 Y 坐标，只用于挑选行矩形，不再喂给 ProseMirror 的坐标 API）。

`npm run check` 0 错误（本轮无 Go 改动）。`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出。三点里第 2、3 点都是纯几何/坐标计算修复，逻辑上应该稳定，但同样受限于本环境无法用鼠标实机验证，需要用户实机确认：1）手柄按钮明显变大好点了；2）拖拽时能看到一条贯穿整行宽度的蓝色粗线跟着鼠标移动；3）在项目列表的文字和符号之间来回移动鼠标，手柄不再跳动。块拖拽本身"能不能真的拖动成功"仍待前几轮加的诊断日志定位，本轮改动没有触碰 `updateDropTarget`/`commitBlockMove` 里判断成功与否的核心逻辑（只是把命中后蓝线怎么画换了实现方式），如果这轮实测蓝线出现了但松手仍然不生效，还是需要读 `debug.log`。

## 2026-07-24（第五次续）：`### ` 和 `/` 快捷键失效的真实根因 + 菜单飘移 + 提示文案

用户反馈"`D:\MDNote\123.md` 后面几行输入 `###` 无法切标题、按 `/` 也不生效"。直接读了这个文件的实际内容（不是仓库里的文件，是用户真实笔记，在配置的根目录 `D:\MDNote` 下）：

```
文本

# 标题１

＃＃＃＃　标题４

#### 

#### １
```

第 3 行 `# 标题１` 是纯 ASCII `#` + 半角空格，成功转成了 H1；第 5/9 行的 `＃＃＃＃` 却是**全角**数字井号（U+FF03），根本不是 ASCII `#`。这就是根因：Windows 中文输入法通常有一个"全角/半角标点"切换（常见绑定 Shift+Space，很容易手滑碰到），切到全角状态后敲键盘上的 `#`/`/` 打出来的是全角字符 `＃`/`／`，而 `@tiptap/extension-heading` 内置的标题输入规则（`textblockTypeInputRule` 用的正则是字面量 `#{1,6}`）和 `Editor.svelte` 里 `detectSlashOpen` 的 `/` 判断都只认 ASCII 半角字符，全角版本完全不触发、也没有任何报错提示——不是这几行"失效"，是这几行打出来的根本不是程序认识的那个字符。这不是 bug，是用户当时输入法处于全角标点状态；但考虑到中文用户很容易在打字过程中不小心切到全角，加了一个防御性兼容：

- 新增 `frontend/src/lib/editor/nodes/fullwidthShortcuts.ts`：`FullwidthHeadingShortcut` 扩展，仿照 `@tiptap/extension-heading` 自己的 `addInputRules`（同样的 `{1,level}` 范围正则技巧），额外注册一组识别全角 `＃` 连续序列的标题输入规则，和内置的 ASCII 版本并存（不是替换）。已接入 `Editor.svelte` 的 extensions 列表。
- `detectSlashOpen` 里 `/` 的判断从 `text[offset-1] !== "/"` 改成同时接受 `"／"`（U+FF0F 全角斜杠）。
- 没有对"全角空格"做类似兼容——JS 正则 `\s` 本身就包含 `\u3000`（全角空格），所以 heading 规则里"后面跟空格"这一半原本就已经能匹配全角空格，只有 `#` 本身这个字符是卡点。
- 没有做成"全局把所有全角标点强制转半角"这种大范围改动——中文用户的正文里大量使用全角逗号/句号/问号等是完全正常、故意的，只针对这套编辑器自己认识的两个结构化触发字符（`#`、`/`）单独加宽容。

用户同时反馈另外两点：

1. **点击行前的当前格式图标弹出菜单后，鼠标只要在正文区域移动，菜单会跟着上下飘**：根因是 `onContentMouseMove` 不管菜单是否打开都会持续根据鼠标当前位置重新计算 `hoverBlockPos`/`hoverClientY` 并调用 `updateHandle()`，而菜单的 `top` 又是 `handleTop + handleHeight + 2` 算出来的——鼠标一移动（哪怕是移向菜单本身，菜单在 contenteditable 区域之外，事件仍然会在父级 wrapper 上触发 mousemove），`handleTop` 就跟着挪，菜单跟着挪。修复：`onContentMouseMove` 加 `menuOpen` 到提前返回的判断里，菜单开着的时候完全冻结悬停追踪，菜单固定在打开那一刻对应的块上，直到点击别处触发 `<svelte:window onclick>` 里已有的关闭逻辑。

2. **灰色占位提示文案去掉"或空格"**：`Placeholder.configure` 的文案从 `"按 "/" 或空格快速插入格式"` 改成 `"按 / 快速插入格式"`（顺带去掉了两侧全角引号，只留 ASCII 斜杠，呼应上面全角字符的主题——提示文案本身也不该用全角符号指代一个只认半角的快捷键）。

`npm run check` 0 错误，`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出。全角输入法兼容这条是根据读到的真实文件内容直接定位到根因的，逻辑清楚（正则表达式层面的问题，不依赖运行时环境），信心比较高；另外两点是常规 DOM/状态修复，也不依赖手势细节，但仍需要用户实机确认菜单不再飘移。

## 2026-07-24（第六次续）：超链接系统浏览器打开 + 内容区缩放 + 大纲跳转动画

用户提了 3 点（第 3 点消息内容为空，未追加具体要求，跳过）：

1. **超链接悬停小手光标 + 点击调用系统浏览器**：`@tiptap/starter-kit` v3 默认自带 `@tiptap/extension-link`（`openOnClick: true`），点了链接会走浏览器原生 `window.open`——但这是在 WebView2 里，`window.open` 大概率是新开一个内嵌 WebView 窗口而不是用户的系统默认浏览器。`Editor.svelte` 里 `StarterKit.configure({ link: { openOnClick: false } })` 关掉默认行为，改在 `links.ts` 的 `LinkClickHandler` 扩展里新增一个 `handleDOMEvents.click`：命中真实 `<a>` 元素（排除已经有自己点击逻辑的 `.link-chip`——那是内部页面/文件链接，不是这里要处理的外部 URL）就 `preventDefault` 并调用 `api.openWithDefaultApp(href)`——这个方法本来就是给"文件链接"用的（`app.go` 里就是 `cmd /c start "" path`），Windows 的 `start` 命令本身对 URL 也通用，直接复用不用新增 Go 方法。順带给 `.tiptap a:not(.link-chip)` 加了 `color:var(--accent)` + 下划线 + `cursor:pointer`（之前完全没有超链接的专属样式，纯文本颜色，容易看不出是个链接）。
2. **正文区域 Ctrl+滚轮缩放，50%~150%**：新增 `editorZoom`（$state，默认 100，10% 步进），`.editor-scroll` 加 `onwheel` 处理器，`ctrlKey` 按住时 `preventDefault()` 并按滚轮方向加减。缩放只套在 `.editor-content-col`（真正的 `.tiptap` 可编辑内容）本身，**没有**套在 `wrapperEl`/`.editor-content-col-wrapper` 这层——原因是 `handle-group`/下拉菜单/拖拽幽灵/蓝色插入线这些浮层都是通过 `getBoundingClientRect()` 算出的"真实像素"差值直接写 `top`/`left` 内联样式，如果这些浮层恰好也在被 `zoom` 的祖先节点内部，CSS `zoom` 会对子树里所有长度值（包括这些已经是"真实像素"的内联 px）再叠加一次缩放，导致浮层随缩放比例偏移错位；只缩放 `.editor-content-col`（浮层的兄弟节点，不是祖先）就不会有这个问题，`getBoundingClientRect()` 本身返回的坐标不受局部 `zoom` 影响（浏览器换算过），后续算 `handleTop`/`dropIndicatorTop` 这些代码完全不用改。
3. **大纲跳转改成"滑动"而非瞬移，且不是全程滑动而是取半屏距离**：`editorBridge` 新增 `scrollToPos`（由 `Editor.svelte` 在 `onMount` 挂上、`onDestroy` 卸载，因为大纲拿不到 `Editor.svelte` 私有的 `scrollEl`），`Outline.svelte` 的 `jump()` 从直接调用 ProseMirror 的 `scrollIntoView()`（瞬间跳转）改成调这个桥接方法。`Editor.svelte` 新增同名函数：算出目标滚动位置后，若和当前滚动位置的距离超过"半屏"（`scrollEl.clientHeight / 2`），先把 `scrollTop` **瞬间**跳到"距目标还差半屏"的位置，再对最后这半屏距离用 `scrollEl.scrollTo({ top, behavior: "smooth" })` 做平滑滚动——这样无论标题隔多远，动画本身的可视距离和耗时都是恒定的一小段（半屏），不会出现"跳到文档很远的标题时，屏幕内容飞速滚过一大段无关内容"这种真实用户要求里提到的"混乱"观感；如果目标本来就在半屏距离内，则直接对这段短距离做平滑滚动，不做瞬间跳转那一步。

`npm run check` 0 错误，`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出。三点都是新功能非 bug 修复，需要用户实机确认：1）鼠标移到超链接上出现小手指针，点击后系统默认浏览器打开对应网址；2）鼠标悬停正文、按住 Ctrl 滚动鼠标滚轮，内容平滑放大缩小、到 50%/150% 停止不再继续；3）点击右侧大纲任意标题，编辑区平滑滑动过去（而不是瞬间跳转），且滑动看起来是"最后一小段"在动而不是整个可视区域高速掠过。

## 2026-07-24（第七次续）：链接打开报错 + 双击选中溢出 + 缩放范围 + 缩放显示 + 跳转动画感知度 + 启动恢复标签页

一口气处理了用户报的 6 点反馈：

1. **点击链接弹出 cmd 窗口报错"找不到文件'@'"**：根因是上一轮把链接打开复用了 `OpenWithDefaultApp`（`cmd /c start "" path`）——`cmd.exe` 会对整条命令行重新做一次它自己的 shell 元字符解析（`&`、`|`、`^` 等在 URL 查询参数里很常见），把 URL 错误拆开导致 `start` 收到的"文件名"变成一截无意义的片段。改用 Wails 官方 `runtime.BrowserOpenURL`（`app.go` 新增 `OpenURL` 方法，底层 Windows 实现走 `github.com/pkg/browser`，不经过 `cmd.exe` 解析，有 `ShellExecute` 兜底），`api.ts` 加 `openURL`，`links.ts` 里外部超链接点击改调这个而不是 `openWithDefaultApp`（内部文件链接 chip 仍用后者，行为不变）。
2. **双击行内代码文字，选区看起来比文字本身多出一截**：读了用户截图（`D:\Download\1.png`，双击"TaskCreate"这个行内代码词，蓝色选区右侧多出一块没有文字的高亮），判断是 `.tiptap code` 的横向内边距（`padding:0.1em 0.35em`）在整词被选中时被浏览器原生选区高亮一并渲染进去——双击选中的文字范围本身没问题，是代码块的装饰性留白视觉上被"连带选中"了。把横向 padding 收窄到 `0.15em`，缩小这个视觉误差（这是渲染层面的固有效果，不是能百分之百消除的东西，只能减小观感）。
3. **Ctrl+滚轮缩放应该让整个正文区域跟着变宽，而不是被 1000px 列宽卡住**：上一轮 `zoom` 只套在 `.editor-content-col`（实际可编辑内容），它的宽度仍然被外层 `.editor-content-col-wrapper` 的 `max-width:1000px` 卡死，缩放只是在同样宽的列里塞进更大的字，越大越挤。这次把 `zoom` 挪到 `wrapperEl`（`.editor-content-col-wrapper`）本身——这样它自己的 `max-width`/`padding` 也跟着缩放系数一起放大缩小，150% 时实际渲染宽度变成约 1500px，是真正意义上的"整个区域缩放"。副作用：`handle-group`/"+"菜单/斜杠菜单/蓝色插入线这几个浮层本来就是 `wrapperEl` 的子元素（这样它们才能跟着内容一起滚动），既然 `wrapperEl` 现在也被 zoom 了，这些浮层已经是"真实像素"的内联 `top`/`left`（`getBoundingClientRect()` 差值算出来的）会被 CSS zoom 再叠乘一次缩放系数，导致浮层随缩放比例整体跑偏。修复：新增 `zoomScale = $derived(editorZoom/100)`，这几处内联像素样式在写入前都先除以 `zoomScale` 抵消掉这次多余的缩放（浏览器渲染时再乘回去，最终视觉位置和缩放前完全一致）。
4. **底栏右下角显示缩放比例，点击重置 100%**：`editorBridge` 新增 `zoom`（$state，由 Editor.svelte 一个 `$effect` 同步 `editorZoom` 过来）和 `resetZoom` 回调（同 `scrollToPos` 一样在 onMount/onDestroy 挂卸）。`StatusBar.svelte` 字数统计右边加了一个按钮显示 `{zoom}%`，点击调 `resetZoom()`（内部就是 `editorZoom=100`），布局从单个 `<span>` 改成 `justify-content:space-between` 让这个按钮天然贴右边。
5. **大纲跳转动画不够明显**：上一轮用的是浏览器原生 `scrollTo({behavior:"smooth"})`，Chromium 自己按距离决定动画时长，短距离（本来就设计成最多滑半屏）很可能在一两帧内就滚完，肉眼几乎看不出。改成手写 `animateScrollTop`：`requestAnimationFrame` 循环 + 固定 320ms 时长 + ease-out 三次方缓动（`1-(1-t)^3`），不再依赖浏览器自己的启发式时长，保证无论多短的距离动画都能被感知到，同时 320ms 本身也不长，不会显得拖沓。
6. **启动时自动恢复上次打开的所有文件（未删除/移走的），缺失的文件不弹错误提示**：`settings.go` 的 `Settings` 加 `OpenTabPaths []string` + `ActiveTabPath string`，INI 格式追加多行 `Tab=<path>`（每个打开文件一行）+ 一行 `ActiveTab=<path>`；`app.go` 新增 `SaveOpenTabs(paths, activePath)`（load-modify-save，不清空 RootDir/Maximized 等其它字段）。前端 `appState.persistOpenTabs()`：收集所有"有 path 的标签"（未保存的空白标签本来就没东西可恢复，不需要记）+ 当前激活标签的 path，调 `api.saveOpenTabs`；接到了 `newTab`/`openPath`（含去重命中分支）/`closeTabImmediately`/`renameEntry`/`moveEntry`/`migrateRootDir`（这几处都会改变"当前打开的文件集合"或某个文件的路径）以及 `Tabs.svelte` 的 `select()`（点击切换标签）——不只在退出时存一次，每次改动都立即写盘，这样即使异常崩溃也不会丢失"最后已知状态"。`openPath` 新增 `opts.silent` 参数，`appState.init()` 启动时先用 `api.fileExists` 逐个探测持久化列表里的路径，只对存在的路径以 `silent:true` 调 `openPath`（跳过的直接忽略，不弹"打开失败"）——原有的双击文件启动（`initialFile`）逻辑保留并优先夺焦点；如果都没有（新装、或者上次就是空标签）才退回 `newTab()`。

`go build`、`npm run check` 均 0 错误，`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（构建前照例先杀掉在跑的 `mdnote`/`mdnote2` 进程）。六点里 1（cmd 报错）和 6（恢复标签）的逻辑相对能通过代码走查确认；2/3/4/5 都是纯视觉/动画效果，仍然依赖用户实机验证：双击代码词看选区是否更贴合文字、Ctrl+滚轮缩放时整个正文区域（而不只是文字）是否明显变宽、状态栏是否出现缩放百分比按钮且点击能重置、大纲跳转的滑动是否肉眼可见但不拖沓、重启软件后之前开着的标签页是否都自动回来了。

## 2026-07-24（第八次续）：确认块拖拽已修复，清理诊断埋点

用户确认最新 `mdnote.exe` 上块拖拽（⠿ 手柄拖动内容块）已经能正常工作——从第五轮起悬了三轮的诊断任务终于关闭。`build\bin\debug.log` 里最后一条记录的时间戳早于当前 exe 的构建时间，且日志文案格式和当前源码对不上（旧版没有 `indicatorTop` 字段），说明那是第四轮那次"整行覆盖层 + `dropIndicatorTop`"改动之前留下的旧记录，后续用户没再触发过新的日志——间接印证第四轮修复之后就已经好了，只是没在当轮拿到确认。

清理了三处诊断代码（均为专门给这个 bug 加的临时埋点，功能确认后按计划移除）：
- `app.go` 删除 `DebugLog` 方法，连带清掉变成未使用的 `"time"` import。
- `frontend/src/lib/api.ts` 删除 `debugLog` 包装。
- `frontend/src/lib/components/Editor.svelte` 的 `updateDropTarget`/`commitBlockMove` 删除全部 `api.debugLog(...)` 调用，提前返回分支保留（逻辑不变，只是不再记日志）。
- 删除残留的 `build\bin\debug.log` 文件。

`frontend/src/lib/wailsjs/wailsjs/go/main/App.d.ts`/`App.js`（wails 自动生成、但前端实际未 import 使用，`api.ts` 是手写绑定）里仍留有 `DebugLog` 签名，因为没有实际引用不影响编译，未做处理。

`go build`、`npm run check` 均 0 错误，`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（构建前已 `Stop-Process -Force` 杀掉在跑的 mdnote 进程）。本轮只是清理，未改动任何功能逻辑，之前几轮里还需要用户实机确认的项目（双击代码词选区、Ctrl+滚轮缩放整体变宽、缩放百分比按钮、大纲跳转动画、启动恢复标签页）不受影响。

## 2026-07-24（第九次续）：Ctrl+±/0 缩放快捷键 + 链接 hover 显示完整地址

用户提了 3 点，第 3 点消息内容为空（未追加具体要求），本轮只处理前两点：

1. **Ctrl+/- 按 10% 步进缩放，Ctrl+0 重置为 100%**：`Editor.svelte` 新增 `onWindowKeydown`，挂到 `<svelte:window onkeydown>`（原来只有 `onclick`）。用 `e.code`（而非 `e.key`）判断 `Equal`/`NumpadAdd`、`Minus`/`NumpadSubtract`、`Digit0`/`Numpad0`——用物理键位而不是字符，避免 Shift+Equal 才是 `+` 这种要求同时按 Shift 的问题（`+`/`-`/`0` 在美式键盘上本来就和 `=`/`-`/`0` 是同一物理键）。复用了已有的 `ZOOM_MIN`/`ZOOM_MAX`/`ZOOM_STEP`/`resetZoom()`，和 Ctrl+滚轮走同一套缩放状态（`editorZoom`），互不冲突。`e.preventDefault()` 顺带压掉了 WebView2/Chromium 自己对这几个组合键的默认页面缩放行为。

2. **链接 hover 显示完整地址**：区分了两种链接——`links.ts` 里 Notion 风"页面/文件链接"chip（`a.link-chip`）本来就已经在 `renderHTML` 里写了 `title: node.attrs.path`，靠浏览器原生 `title` 属性早就有 hover 提示，不用改。真正缺的是纯文本超链接（StarterKit 自带的 `Link` mark，`a:not(.link-chip)`）——它是 mark 不是自定义 node，renderHTML 走的是 `@tiptap/extension-link` 内置逻辑，不方便每个实例单独注入含 href 值的 `title` 属性。改用纯 CSS 方案：`.tiptap a:not(.link-chip)` 加 `position:relative`，新增 `:hover::after { content: attr(href); position:absolute; top:100%; ... }`——`attr(href)` 直接读 DOM 上已有的 `href` 属性值，不需要 JS 参与，样式抄的 `.block-drag-ghost` 那套配色（`--content-bg`/`--border`/`--text-primary` + 阴影），保证跟随浅色/深色主题。因为是纯 CSS、挂在链接自身而非用 JS 算绝对坐标，天然不受 `wrapperEl` 的 `zoom:${editorZoom}%` 缩放影响（不像 `handleTop`/`dropIndicatorTop` 那些用 `getBoundingClientRect()` 算出真实像素再需要除以 `zoomScale` 抵消的浮层）。

第 3 点用户没写内容，跳过，已在回复里提醒用户如果有别的需求需要补充说明。

`npm run check` 0 错误（本轮无 Go 改动），`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（构建前已杀掉在跑的 mdnote 进程）。两点都需要用户实机确认：1）光标在编辑区内，按 Ctrl+`=`/Ctrl+`-` 能像滚轮一样 10% 步进缩放，到 50%/150% 停住，按 Ctrl+`0` 直接跳回 100%；2）鼠标悬停在正文里的普通超链接（不是页面/文件链接 chip）上，链接下方浮现一个显示完整网址的小气泡，深浅主题下都清晰可读。

## 2026-07-24（第十次续）：链接提示字号跟随格式 + 查找/替换焦点 + 跳转不可见 + 按钮太浅

用户提了 4 点：

1. **链接 hover 提示的字体/字号不该跟随所在文字的格式（标题里的链接提示会跟着变大变粗）**：上一轮加的 `::after { content: attr(href); }` 只显式设了 `font-size`，`font-weight`/`font-style` 没设，链接如果落在标题（大字号+粗体）或加粗文字里，`::after` 伪元素默认从 `<a>` 的计算样式继续往上继承这两项，提示气泡跟着变大变粗。补上 `font-weight: normal`、`font-style: normal`、`font-family: inherit`（这条其实本来就会自然继承，写出来只是明确锁死，防止以后哪个块类型意外改了 `font-family`），保证提示气泡的字体观感在任何上下文里都固定一致。

2. **点击"查找"或 Ctrl+F 后，焦点应该在查找输入框**：原来的 `<input autofocus>` 只在元素首次挂载时生效一次——面板已经打开时再按一次 Ctrl+F，或者已经打开查找、再点一次工具栏按钮，`{#if}` 块不会重新创建输入框，`autofocus` 不会重新触发。改成显式聚焦：`appState` 新增 `openFindReplace(focusReplacement)` 方法（设置 `findReplaceOpen=true` + `findFocusReplacement` + 递增一个 `findFocusNonce` 计数器），`FindReplace.svelte` 新增一个依赖 `findFocusNonce` 的 `$effect`，每次 nonce 变化都显式 `.focus()` + `.select()` 对应输入框——用 nonce 而不是只依赖 `findReplaceOpen` 这个布尔值本身，是因为"面板已经开着，再按一次 Ctrl+F"这种情况下 `findReplaceOpen` 值没变化，`$effect` 不会重新触发，只有额外的计数器变化才能保证每次按快捷键都真正重新聚焦一次。

3. **新增：Ctrl+H 呼出替换，交互逻辑和 Ctrl+F 一致**：`+page.svelte` 的全局快捷键新增 `h` 分支，调 `appState.openFindReplace(true)`（聚焦"替换为"输入框而不是"查找"）。工具栏原来"查找"和"替换"两个按钮共用同一个 `toggleFind`（只是开关面板，不区分焦点目标），顺手也拆开：查找按钮聚焦查找框、替换按钮聚焦替换框，逻辑和快捷键保持一致。原来"再点一次按钮就关闭面板"的开关行为没有保留——改成参照浏览器 Ctrl+F 的常见习惯：不管当前是否已经打开，都确保打开并聚焦；面板关闭仍然可以用查找框自带的 Escape 键或"关闭"按钮。

4. **查找的上一个/下一个应该让内容区跟着滚动过去 + 上一个/下一个/替换/全部替换/关闭这几个按钮颜色太浅**：
   - 定位跳转问题：原来的 `scrollToActive()` 用的是 ProseMirror 自带的 `.setTextSelection(...).scrollIntoView()` 命令——这个命令会把编辑器的真实光标/选区移动到匹配位置，而用户此刻焦点大概率还停留在查找输入框里（尤其是刚做完上一条"焦点应该在输入框"的修复之后），移动编辑器内部选区不会带来任何可见的视觉滚动反馈（并且这个副作用本身也有点怪——不该在用户还在打字查找时偷偷把编辑器的编辑光标挪走）。改成复用大纲跳转那套已验证好用的 `editorBridge.scrollToPos` 机制：从 `scrollToPos` 里拆出公共的 `slideScrollTo(top)`（算目标滚动位置 + 超过半屏就先瞬间跳到差半屏的位置 + 剩下一小段做 320ms 缓动动画），新增 `scrollToRange(from, to)` 复用这个滚动逻辑但不调用 `.focus()`/`.setTextSelection()`，通过 `editorBridge.scrollToRange` 暴露给 `FindReplace.svelte`。这样查找跳转到哪、内容区就真的滚动过去，同时不会抢用户在输入框里的焦点。
   - 按钮颜色：`.row button`（上一个/下一个/关闭三个纯图标按钮）默认色从 `--text-secondary`（浅灰）改成 `--text-primary`（深色），hover 色从 `--text-primary` 改成 `--accent`（蓝色，比原来更醒目的强调色，和 hover 时机呼应）；"替换"/"全部替换"两个文字按钮加 `.primary` 样式（蓝底白字，抄的 `ConfirmDialog.svelte` 里 `.primary` 按钮同款配色），从"边框按钮"升级成实心强调按钮，视觉重量和"这是要执行写操作的按钮"这件事更匹配。

`npm run check` 0 错误（本轮无 Go 改动），`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（构建前已杀掉在跑的 mdnote 进程）。全部 4 点都需要用户实机确认：1）在加粗标题里放一个链接，hover 提示气泡的字看起来和正文一样细/一样大，不再变粗变大；2）点查找按钮或按 Ctrl+F，光标立即能在查找框里打字，不用再手动点一下；3）按 Ctrl+H，光标直接落在"替换为"框；4）多按几次"下一个"，能看到内容区跟着滑动到匹配位置，以及查找面板几个按钮的颜色是否明显更深/更醒目了。

## 2026-07-24（第十一次续）：查找面板收尾 + 取消空格唤起菜单 + 空白区域双击定位 + 多选块级行

用户提了 6 点，第 6 点其实是对第 5/6 点提到的多选功能的确认追问，实际按 5 项处理；先用 `AskUserQuestion` 确认了两处容易走偏的地方：

- 块级多选具体要支持哪些操作——用户选了"整体删除/剪切/复制"+"整体拖拽移动"两项都要，不是只做视觉高亮。
- Ctrl+Y 是否已经生效——查了 `node_modules/@tiptap/starter-kit` 源码，`StarterKit` 默认就带 `@tiptap/extensions` 的 `UndoRedo` 扩展，其中 `"Mod-y": () => this.editor.commands.redo()` 已经绑定，`+page.svelte`/`Editor.svelte` 的全局按键处理都不拦截字母 `y`，判断这条**已经生效，本轮未改代码**。

其余 5 点：

1. **查找/替换面板"关闭"按钮加大 + 布局调整**：原来的关闭按钮和"上一个/下一个"挤在查找那一行末尾，跟随行内容左右移动。改成绝对定位固定在整个面板（查找行+替换行两行的外层容器）右上角，尺寸从 24px 提到 28px、图标从 14 提到 16；顺手把"上一个/下一个"、"替换/全部替换"分别包进一个 `.nav-group` 小容器统一间距，视觉上从"松散的几个按钮"变成两组清晰的按钮群。
2. **取消"空白行按空格弹出格式菜单"**：删掉 `slashTrigger.ts` 里专门监听空格键的那段 `handleKeyDown` 分支（连带清理了因此变成未使用的 `$from`/`empty` 解构），现在只有输入 `/`（或全角 `／`）才会唤起格式菜单。`Editor.svelte` 里那个专门为空格触发路径定位菜单的 `$effect`（因为空格路径当时无法直接调用 `positionSlashMenu`）也一并删除——`detectSlashOpen()`（"/"触发的唯一入口）本来就已经在触发时直接调用 `positionSlashMenu`，这个 `$effect` 除了空格路径外没有其它调用方，是彻底的死代码。
3. **空白区域双击快速定位光标**：编辑区可编辑内容（`.tiptap`）本身不带 padding，实际的 56px/64px/30vh 内边距是包在外层 `.editor-content-col-wrapper`（非 contenteditable 的普通 div）上的——鼠标点在这圈留白里原生浏览器不会做任何事，此前只能反复按 Enter（=不断插入新段落）"走"到目标行，纯属能凑合但会脏改文档内容的变通做法。新增 `onWrapperDblClick`：双击若落在这圈留白（用 `target.closest(".tiptap, ...")` 排除真正点在正文/悬浮控件上的情况）就用 `updateDropTarget` 同款的"坐标钳制进 `editor.view.dom` 包围盒再 `posAtCoords`"手法算出最近的文档位置，复用已有的 `placeCursorNear()`（原本是给分割线插入之类场景做"跳过 atom 节点落到最近可编辑位置"用的）把光标真正落过去。
4. **鼠标多选块级行 + 蒙层高亮 + 支持整体删除/剪切/复制/拖拽**：参照 `D:\Download\2.png`（Notion 风格：每一行浅蓝色圆角背景 + 跨多行的选中蒙层），新增了一套"边距拖选"机制：
   - `onMarginPointerDown` 挂在 `.editor-content-col-wrapper` 上，鼠标在留白区域（同上一条的排除逻辑）按下并拖动过阈值后，持续用同款钳制坐标算出"起点/当前点"所在的整块范围，存进 `multiSelectRange`（`{from, to}`，两端永远对齐到整块边界），配套算出每个被覆盖的顶层块的真实 DOM 矩形存进 `multiSelectRects`，渲染成一组浅蓝圆角覆盖层（新增 CSS 类 `.row-highlight`，和已有的 `handle-group`/`block-drop-indicator` 一样是绝对定位、JS 算坐标的独立浮层，不依赖某个具体块类型自己的背景/伪元素）。
   - 松手后 `finalizeMultiSelect()` 把这个范围真正设成编辑器的 `TextSelection`（`editor.chain().focus().setTextSelection(multiSelectRange).run()`）——这样 Backspace/Delete/Ctrl+C/Ctrl+X 直接是 ProseMirror/浏览器原生行为，不需要另写剪贴板或删除代码；原生选区自己的矩形高亮视觉上没法做出圆角"整行"的效果，所以新增 `.editor-content-col-wrapper.multiselect-active :global(.tiptap ::selection){background:transparent}`，只隐藏原生高亮的显示，不影响它作为真选区被拖拽/键盘操作读取。用一个 `applyingMultiSelectSelection` 标志位区分"这是我们自己刚设的选区"还是"用户点别处/打字产生的新选区"，后者会在 `onSelectionUpdate` 里清掉高亮（点别处 = 取消多选，是预期行为）；切换标签页时也会显式清掉（位置引用的是旧文档，切文档后失效）。
   - 整体拖拽移动：`onDragHandlePointerDown` 抓取 `⋮⋮` 手柄时先判断当前块是否落在活跃的 `multiSelectRange` 内，是的话 `dragSource` 直接用整个多选范围而不是单个块；`commitBlockMove` 相应从"取单个 `doc.nodeAt(source.from)` 节点插入"改成"取 `doc.slice(source.from, source.to)` 的 `Fragment` 插入"，单块拖拽（原有功能）和多块拖拽现在走同一份代码，因为两者本质都只是"一段 `[from,to)` 的整块内容搬到别处"。
5. **落地行也用同一套高亮**：把上面新增的 `.row-highlight` 样式顺带用在块拖拽的落地目标上——`updateDropTarget` 新增 `dropTargetRect`（目标块的真实矩形，和已有的 `dropIndicatorTop` 那条细线并存），模板里多渲染一个 `.row-highlight`，这样拖拽时不仅有一条线标出精确插入点，目标行本身也会整行泛蓝，更容易看出"挨着哪一行"。

`npm run check` 0 错误（本轮无 Go 改动），`wails build -ldflags "-s -w" -o mdnote.exe` 重新产出（构建前已杀掉在跑的 mdnote 进程）。多选相关的三点（4、5，以及双击定位的第 3 点）都是这轮新写的、涉及具体鼠标手势的功能，逻辑走查完成但本环境无法模拟真实拖拽/双击手势验证，风险敞口最大，需要用户重点实机测试：a) 双击文档末尾空白或行两侧留白，光标能准确落到最近的那一行；b) 在留白区从某一行拖到另一行，途经的每一整行是否都变浅蓝、松手后原生选区是否已经能正常 Backspace/Ctrl+C/Ctrl+X；c) 拖住选中范围内任意一行的 `⋮⋮` 手柄，能否把整组一起挪动到新位置；d) 单块拖拽时，落地的那一整行是否也泛蓝高亮（而不只是一条线）。另外 1、2 两点风险较低（纯样式调整/删除已有分支），仍建议一并确认。
